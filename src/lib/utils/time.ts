export const MS_PER_DAY = 24 * 60 * 60 * 1000;

/** Days until deadline is shown as danger (red) */
export const DEADLINE_CRITICAL_DAYS = 7;

/** Days until deadline is shown as warning (orange) */
export const DEADLINE_WARNING_DAYS = 14;

/** Upcoming deadline window shown in dashboard */
export const DEADLINE_UPCOMING_DAYS = 30;
